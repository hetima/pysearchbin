from setuptools import setup, find_packages

setup(
    name='pysearchbin',
    version="0.0.1",
    description="searching within binary files",
    author='hetima',
    packages=find_packages(),
    license='BSD 2-Clause'
)
